Installation
############

The package is published on `PyPI <https://pypi.org/project/py-app-dev/>`_ and can be installed with `pip` (or any equivalent):

.. code-block:: bash

   pip install py_app_dev
